#include <stdio.h>

/**
 * _strlen - lenth of a string.
 * @s: Address of the string.
 * Return: String length.
 */

int _strlen(char *s)
{
	int count;

	for (count = 0; s[count] != '\0'; ++count)
	return (count);
}
